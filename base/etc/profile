# ThaiOS System Profile

if [ -d /etc/profile.d ]; then
  for i in /etc/profile.d/*.sh; do
    if [ -r $i ]; then
      . $i
    fi
  done
  unset i
fi

export THAIOS_VERSION="1.0"
export THAIOS_CODENAME="Songkran"
